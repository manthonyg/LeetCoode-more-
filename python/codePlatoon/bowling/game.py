# frame_list = [frame1,frame2, ......, frame10]


def check_last_frame(self, frame1):
        if frame.spare:
            # do something
        elif frame.strike:
            # do something
        else:
            # do nothing

